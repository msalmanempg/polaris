{
  "name": "@polaris/domain",
  "version": "1.0.0",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "private": true,
  "scripts": {
    "build": "ttsc -p tsconfig.app.json",
    "lint": "eslint --ext .js,.ts ./",
    "lint:errors": "eslint --quiet --ext .js,.ts ./",
    "lint:fix": "yarn lint --fix",
    "lint:types": "ttsc --noEmit",
    "test": "jest",
    "test:ci": "jest --ci --coverage",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@polaris/prisma": "1.0.0",
    "injection-js": "^2.4.0",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/faker": "^5.1.7",
    "@types/jest": "^26.0.20",
    "faker": "^5.4.0",
    "jest": "^26.6.3",
    "jest-mock": "^26.6.2",
    "jest-mock-extended": "^1.0.13",
    "mockdate": "^3.0.2",
    "ts-jest": "^26.5.2",
    "ttypescript": "^1.5.12",
    "typescript": "^4.2.2"
  }
}
